Algoritmo "EXERCICIO ESTRUTURA PARA - SEQUENCIA ÍMPAR"

Var

  x, i      :inteiro

Inicio

   escreva("Digite o valor de X: ")
   leia(x)
   
   para i de 1 até x faça
        //verifica os numeros da variavel i que são ímpares e
        //apresenta o resultado mostrando eles na tela
        se i % 2 <> 0 então
           escreval(i)
        fimse
   fimpara

   

Fimalgoritmo