Algoritmo "Loop repita - multiplicação de numeros"

Var

i, num, conta: inteiro

Inicio

conta <- 1

repita
      escreva("digite um numero: ")
      leia(num)
      conta <- conta*num
      i <- i + 1
até i >= 5
escreva("O resultado da multiplicação dos numeros é de", conta)

Fimalgoritmo